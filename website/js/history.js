/**
 *
 * by littlefean
 */

{
    // let arr = document.getElementsByClassName("mdItem");
    // for (let item of arr) {
    //     // todo 待废弃
    //     item.style.setProperty("left", `${Math.random() * 80 + 20}%`);
    // }
}
